export async function exportExcel(name:string,rows:object[]) { const XLSX = await import("xlsx"); const book=XLSX.utils.book_new(); XLSX.utils.book_append_sheet(book,XLSX.utils.json_to_sheet(rows),name.slice(0,31)); XLSX.writeFile(book,`${name}-${new Date().toISOString().slice(0,10)}.xlsx`); }
export async function exportPDF(name:string,headers:string[],rows:(string|number)[][]) { const { jsPDF } = await import("jspdf"); const { default: autoTable } = await import("jspdf-autotable"); const doc=new jsPDF(); doc.setFontSize(16); doc.text(name,14,18); autoTable(doc,{head:[headers],body:rows,startY:26,styles:{fontSize:8}}); doc.save(`${name}.pdf`); }
